// Free APIs that don't require registration or API keys

export const API_ENDPOINTS = {
    // OpenStreetMap Overpass API - Points of Interest (completely free, no key)
    overpass: 'https://overpass-api.de/api/interpreter',

    // PredictHQ has a free tier but requires signup
    // We'll use public event aggregators instead

    // Wikipedia Events API (free, no key)
    wikipedia: 'https://en.wikipedia.org/api/rest_v1',
};

// No API keys needed!
export const API_KEYS = {
    // All APIs we use are free and don't require keys
};
